// =======================================================
// Map.jsx — PirateWorld (DESACTIVADO)
// Archivo legacy fuera de flujo actual
// =======================================================

import React from "react";

export default function Map() {
  return (
    <div className="game-content">
      <h2>Mapa</h2>
      <p>Vista no activa en esta versión.</p>
    </div>
  );
}
